<div class="container p-3">
  <!-- 日記一覧 -->
  <div class="row my-4">
   	<div class="col-12">
     	<div class="row">
				<% if @my_diaries.present? %>
          <%= render @my_diaries %>
				<% else %>
					<div class="mb-3">No Diaries</div>
				<% end %>
			</div>
			<%= paginate @my_diaries, theme: 'bootstrap-5'%>
		</div>
  </div>
</div>