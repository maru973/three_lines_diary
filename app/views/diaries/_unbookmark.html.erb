<%= link_to bookmark_path(current_user.bookmarks.find_by(diary_id: diary.id)), id: "unbookmark-button-for-diary-#{diary.id}", data: {turbo_method: :delete} do %>
  <i class="bi bi-suit-heart-fill fs-3 text-danger"></i>
<% end %>