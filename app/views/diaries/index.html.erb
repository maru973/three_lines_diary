<div class="container p-3">
  <!-- 日記一覧 -->
  <div class="row my-4">
   	<div class="col-12">
     	<div class="row">
				<% if @diaries.present? %>
          <%= render @diaries %>
				<% else %>
					<div class="mb-3 text-light">No Diaries</div>
				<% end %>
			</div>
			<%= paginate @diaries, theme: 'bootstrap-5'%>
		</div>
  </div>
</div>
