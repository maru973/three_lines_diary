<%= turbo_stream.replace "comment-#{@comment.id}" do %>
  <%= render 'edit_comment', comment: @comment, diary: @comment.diary %>
<% end %>
<%= turbo_stream.prepend 'table-comment' do %>
  <%= render 'comment', comment: @comment, diary: @comment.diary %>
<% end %>
<%= turbo_stream.replace "comment-#{@comment.id}" do %>
  <%= render 'edit_comment', comment: @comment, diary: @comment.diary %>
<% end %>